# universal-render-php-dockerfile.
# Upload the project to the root directory.
# Revise the “docker-compose.yml”，“Dockerfile” and “test.conf” content to own project name.
# ubuntu:20.04 nginx sqlite redis PHP
